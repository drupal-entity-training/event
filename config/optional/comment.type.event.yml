langcode: en
status: true
dependencies:
  enforced:
    module:
      # @todo Remove this when https://www.drupal.org/node/2717673 lands.
      - event
id: event
label: 'Event comments'
target_entity_type_id: event
description: 'Allows commenting on events'
